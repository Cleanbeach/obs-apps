# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: loki
#   namespace: argocd
#   finalizers:
#   - resources-finalizer.argocd.argoproj.io
# spec:
#   destination:
#     namespace: default
#     server: https://kubernetes.default.svc
#   project: default
#   source:
#     path: charts/loki/
#     repoURL: https://github.com/grafana/helm-charts/
#     targetRevision: HEAD
      helm:
        valueFiles:
        - $values/apps/loki/loki-values.yaml
        - $values/apps/loki/persisten-volume-claim.yaml
      # parameters:
      #   # mode: "deployment"
      #   - name: mode
      #     value: "deployment"
        - repoURL: https://github.com/Cleanbeach/obs-apps.git
          path: apps/loki/
          targetRevision: master
          ref: values